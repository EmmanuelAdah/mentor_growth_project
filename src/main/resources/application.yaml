server:
  port: 8089

spring:
  application:
    name: mentor-growth

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: profile, email
            redirect-uri: ${GOOGLE_OAUTH_REDIRECT_URL}


  datasource:
    url: jdbc:postgresql://localhost:5432/mentor_growth_db
    username: postgres
    password: Emma007_
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  rabbitmq:
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}
    port: 5672
    host: localhost

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

Paystack:
  apiKey: ${PAYSTACK_SECRET}
  uri: ${PAYSTACK_BASE_URI}

jwt:
  secret:
    key: ${JWT_SECRET_KEY}

Cloudinary:
  cloud-name: ${CLOUDINARY_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}

rabbitmq:
  exchange:
    name: mentorship_exchange
  queue:
    name: mentorship_queue
  routing:
    key: mentorship_routing_key

google:
  credentials:
    path: ${GOOGLE_APPLICATION_CREDENTIALS}
