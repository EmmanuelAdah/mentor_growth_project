spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mentor_growth_test
    username: postgres
    password: Emma007.
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
